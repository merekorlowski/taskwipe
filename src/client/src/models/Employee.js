export default class Employee {
    
        constructor(id, name) {
            this.id=id;
            this.name=name;
        }

        edit(id, name) {
            this.id = id;
            this.name = name;
        }
}