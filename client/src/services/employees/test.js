import EmployeeService from './';

describe('Employees API', () => {
	let employeeService = new EmployeeService();

	it('Returns all of the employees in the team t001', (done) => {
		done();
	});
});
